import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import Sidebar from '@/components/layout/Sidebar'
import Topbar from '@/components/layout/Topbar'
import Link from 'next/link'
export default async function SettingsPage(){const supabase=createClient();const {data:zuser}}=await supabase.auth.getUser();if(!user)redirect('/auth/login');const {data:profile}=await supabase.from('profiles').select('*').eq('id',user.id).single();return(<div className="flex h-screen bg-nf-bg overflow-hidden"><Sidebar profile={profile}/><div className="flex flex-col flex-1 overflow-hidden"><Topbar user={user} profile={profile}/><main className="flex-1 overflow-y-auto p-6 lg:p-8"><div className="max-w-2xl mx-auto space-y-6"><h1 className="text-2xl font-bold">Settings</h1><div className="nf-card p-5 space-y-4"><div><label className="text-xs font-mono text-muted-foreground">EMAIL</label><p className="text-sm mt-1">{user.email}</p></div><div><label className="text-xs font-mono text-muted-foreground">PLANQTICKET</label><p className="text-sm mt-1 capitalize text-nf-purple">{profile?.plan||'free'}</p></div></div><div className="nf-card overflow-hidden"><Link href="/settings/billing" className="flex items-center gap-3 p-5 hover:bg-nf-surface2"><div className="flex-1"><div className="text-sm font-medium">Billing &amp; Plans</div></div><span>→</span></Link></div></div></main></div></div>)}\n 